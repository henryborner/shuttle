// testdata fixture: long CJK name. CJK glyphs are 2 columns wide, so width
// must be measured in display columns, not runes, or the bar overflows 80.
// 测试数据：长中文文件名。CJK 字符占 2 列，宽度必须按显示列计算而非字符数，
// 否则进度条会超过 80 列。
中文长文件名测试数据
