// testdata fixture: long file name as seen in real VS Code mermaid cache
// (32 chars). Progress bar line must stay within 80 columns even for this.
// 测试数据：用户真实遇到的长文件名（VS Code mermaid 缓存，32 字符）。
// 即使这样，进度条行也必须保持在 80 列内。
xychartDiagram fixture
