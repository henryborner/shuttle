Agents directory for go:embed — pre-compiled slim remote binaries.

Generated by:  .\build.ps1   (Step 1)
The script cross-compiles shuttle_agent for linux/amd64 and linux/arm64,
places them here, then builds shuttle.exe with go:embed.

These files are gitignored. Run build.ps1 to regenerate them.
